
 LCTuple
 -------

 Marlin package that creates a ROOT TTree with a column wise ntuple
 from LCIO collections.

 F.Gaede, DESY, 2011

-----------------------------------------------------



  Build LCTuple
  -------------

    . /path/to/ilcsoft/installation/v01-XX/init_ilcsoft.sh
    mkdir build
    cd build
    cmake -C $ILCSOFT/ILCSoft.cmake ..
    make install # this should create library: lib/libLCTuple.so
    cd ..


 
 Running LCTuple
 ---------------

    export MARLIN_DLL=$PWD/lib/libLCTuple.so
    cd example
    Marlin --global.LCIOInputFiles=./DST01-06_ppr004_6f_bbqqqq_500_ILD_00_0.001.slcio --global.MaxRecordNumber=1000 lctuple.xml
    

    // make plots with TTree:Draw(), e.g.
    root -l 
    x /example_plots.C("./lctuple.root")


 
-------------------------------------------------------------------------------------------


  --------
 | v00-01 |
  --------

       - first version creates CWNT for 
        MCParticles, ReconstructedParticles, Tracks, Clusters 

      - optionally also SimTrackerHits and SimCalorimeterHits can
        be written to the ntuple
  